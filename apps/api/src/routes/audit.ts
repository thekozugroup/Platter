import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the audit agent (paginated audit feed) lands the real routes here.
const auditRoutes: FastifyPluginAsync = async () => {};

export default auditRoutes;
