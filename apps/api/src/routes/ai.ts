import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the AI agent (provisioning, diagnosis, chat) lands the real routes here.
const aiRoutes: FastifyPluginAsync = async () => {};

export default aiRoutes;
