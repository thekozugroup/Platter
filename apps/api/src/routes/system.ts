import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the system agent (bootstrap, settings, metrics, health) lands the real routes here.
const systemRoutes: FastifyPluginAsync = async () => {};

export default systemRoutes;
