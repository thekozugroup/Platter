import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the backups agent (create, restore, download, rotate) lands the real routes here.
const backupRoutes: FastifyPluginAsync = async () => {};

export default backupRoutes;
