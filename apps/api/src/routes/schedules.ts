import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the schedules agent (cron CRUD + dispatcher) lands the real routes here.
const scheduleRoutes: FastifyPluginAsync = async () => {};

export default scheduleRoutes;
