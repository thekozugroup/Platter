import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the servers agent (CRUD, power, subusers) lands the real routes here.
const serverRoutes: FastifyPluginAsync = async () => {};

export default serverRoutes;
