import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the nodes agent (node CRUD, capacity, health) lands the real routes here.
const nodeRoutes: FastifyPluginAsync = async () => {};

export default nodeRoutes;
