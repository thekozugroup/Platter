import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the console agent (websocket console + command REST fallback) lands the real routes here.
const consoleRoutes: FastifyPluginAsync = async () => {};

export default consoleRoutes;
