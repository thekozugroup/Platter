import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the users agent (admin user management) lands the real routes here.
const userRoutes: FastifyPluginAsync = async () => {};

export default userRoutes;
