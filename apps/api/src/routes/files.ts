import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the files agent (browse, read, write, upload, archive) lands the real routes here.
const fileRoutes: FastifyPluginAsync = async () => {};

export default fileRoutes;
