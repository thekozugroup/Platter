import type { FastifyPluginAsync } from 'fastify';

// Placeholder: the blueprints agent (catalogue + validation) lands the real routes here.
const blueprintRoutes: FastifyPluginAsync = async () => {};

export default blueprintRoutes;
